# chatter-jack
A simple chat application built using Go and ReactJS.

## Quick-start

### Frontend
- `cd` into client 
- run `npm install`
- run `npm start`

### Backend
- `cd` into server
- run `go run main.go`
